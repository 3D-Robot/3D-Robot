# 3D Robot
The goal of 3D Robot is to make a general purpose assembly robot
Intially the software is just to facilitate models from model design to the Robot assembly stage
The models can be modeled in blender then converted into a .3dr file type which will be processed by the computer then made into instructions for the robot
